// React import not required with automatic JSX runtime

const Stats = () => {
  return (
    <div>
      <h1>Stats Page</h1>
      <p>This is the stats page.</p>
    </div>
  );
};

export default Stats;
